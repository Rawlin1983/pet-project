import React from 'react';
import ReactDOM from 'react-dom';
import PetCalorieCount from './PetCalorieCounter';

ReactDOM.render(
    <React.StrictMode>
        <PetCalorieCount />
    </React.StrictMode>,
    document.getElementById('root')
);
